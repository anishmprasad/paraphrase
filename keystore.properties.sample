# Copy to keystore.properties (gitignored) and fill in your own values.
# Create the keystore first — see README, "Publishing to Google Play".
storeFile=paraphrase-release.jks
storePassword=CHANGEME
keyAlias=paraphrase
keyPassword=CHANGEME
